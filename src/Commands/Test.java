package Commands;

public class Test{}
    /*
Player p = (Player)cs;
                double points = Integer.parseInt(args[0]);
                int lvl = p.getLevel();
                int currentpoints = 7 + lvl*2;
                float u = p.getExp();
                
                p.sendMessage(u + "");
                boolean correct = false;
                double finalpoints = points;
                int curr = (int) (currentpoints*(u));
                int lvlfinal = lvl-1;
                do{
                    boolean in = true;
                    if(curr + finalpoints>=currentpoints){
                        lvlfinal++;
                        currentpoints = 7 + 2*lvlfinal;
                        finalpoints -= (1-u)*currentpoints;
                        u = 0;
                        curr = 0;
                        correct = false;
                        in = false;
                    }else{
                        currentpoints = 7 + 2*lvlfinal;
                        if(!in){finalpoints+=(1-u)*currentpoints;}
                        else{}
                        correct = true;
                    }
                }while(!correct);
    */